import React from "react";
import { Component, NavComponent, TabsComponent } from "./style";

export const Tab: React.FC<any> = ({ children, ...props }) => {
   return <Component {...props}></Component>;
};
